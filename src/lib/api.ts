export type RepositoryOverview = { repository_id:string; root:string; languages:string[]; frameworks:string[]; architecture_pattern:string; files_indexed:number; symbols:{name:string;kind:string;file_path:string;line_start:number;line_end:number;signature?:string|null}[] };
export type SecurityFinding = { issue:string; severity:'info'|'low'|'medium'|'high'|'critical'; file_path:string; line:number; evidence:string; recommendation:string };
export type ChatAnswer = { answer:string; citations:string[]; execution_flow:string[] };
const API_BASE = import.meta.env.VITE_API_BASE_URL ?? 'http://localhost:8000';
async function json<T>(path:string):Promise<T>{ const res=await fetch(`${API_BASE}${path}`); if(!res.ok) throw new Error(await res.text()); return res.json(); }
export const api={ analyzeLocal:(path='.')=>json<RepositoryOverview>(`/api/v1/analyze/local?path=${encodeURIComponent(path)}`), chatLocal:(q:string,path='.')=>json<ChatAnswer>(`/api/v1/chat/local?path=${encodeURIComponent(path)}&question=${encodeURIComponent(q)}`), securityLocal:(path='.')=>json<SecurityFinding[]>(`/api/v1/security/local?path=${encodeURIComponent(path)}`), impactLocal:(module:string,path='.')=>json(`/api/v1/impact/local?path=${encodeURIComponent(path)}&module=${encodeURIComponent(module)}`), documentationLocal:(path='.')=>json<{readme:string;setup_guide:string;api_docs:string;developer_guide:string}>(`/api/v1/documentation/local?path=${encodeURIComponent(path)}`)};
